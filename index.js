/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

// for library publish change to this code
export default App;

// for independently running the app
// AppRegistry.registerComponent(appName, () => App);
